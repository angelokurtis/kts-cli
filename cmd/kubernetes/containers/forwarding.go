package containers

import (
	"github.com/spf13/cobra"
)

func forwarding(cmd *cobra.Command, args []string) {
	//containers, err := kubectl.ListContainers(namespace, allNamespaces)
	//if err != nil {
	//	system.Exit(err)
	//}
	//container, err := containers.SelectOnePort()
	//if err != nil {
	//	system.Exit(err)
	//}
	//port, err := pod.SelectContainerPort()
	//if err != nil {
	//	system.Exit(err)
	//}
	//n := pod.Metadata.Name
	//ns := pod.Metadata.Namespace
	//p := strconv.Itoa(port)
	//color.Secondary.Println("kubectl port-forward " + n + " " + p + ":" + p + " -n " + ns)
}
